
input = "?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?"

toCount = '?'

count = 0

for element in input:
    if(element is toCount):
        count = count + 1

print("São " + str(count) + " " + toCount)